import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { {{pascalCase name}} } from '../types'
// import { {{camelCase name}}Api } from '../api/{{camelCase name}}Api'

export const use{{pascalCase name}}Store = defineStore('{{camelCase name}}', () => {
  const items = ref<{{pascalCase name}}[]>([])
  const loading = ref(false)

  async function fetchAll() {
    loading.value = true
    try {
      // items.value = await {{camelCase name}}Api.list()
    } finally {
      loading.value = false
    }
  }

  return { items, loading, fetchAll }
})
