// {{pascalCase name}} module entrypoint
// Re-export only what other modules need to consume directly.
// Example: export { use{{pascalCase name}}Store } from './stores/use{{pascalCase name}}Store'
export {}
